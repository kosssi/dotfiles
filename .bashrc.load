# include what you want

export PATH=$PATH:$HOME/bin

# add this to the .bashrc: . $HOME/.bashrc.load

####
# put any bash customizations in here

alias uuu='composer self-update && cd $NVM_DIR && git pull && uu'
alias uu='sudo apt-get update && sudo apt-get upgrade'
alias cdp='cd $HOME/dev/projects/'
alias debug='export XDEBUG_CONFIG="idekey=phpStorm-idekey"'

export HOSTALIASES=$HOME/.hosts
export RUBY=$HOME/.rvm/bin/ruby

source $HOME/.rvm/scripts/rvm
